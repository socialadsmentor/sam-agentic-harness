# install/

Scripts only. The guided `/harness-install` skill now lives at
`.claude/skills/harness-install/SKILL.md` (Claude Code discovers project skills there, not
under `install/`). Run the scripts directly with `node install/fit-analysis.cjs`,
`node install/install.cjs`, `node install/readback.cjs`, and `node install/rollback.cjs`
from the repo root - see the Quick start section in `README.md` for the full command chain.
